interface:
  display_name: "Web Workers"
  short_description: "MCP task queue, local tools and inline token estimates"
  default_prompt: "Use $web-workers to queue work for the official ChatGPT MCP connector, inspect submitted changes and show estimated token savings, with native Luna max fallback."
policy:
  allow_implicit_invocation: true
