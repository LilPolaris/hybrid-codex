interface:
  display_name: "Web Workers"
  short_description: "Delegate focused reading to Web workers; Codex verifies"
  default_prompt: "Use $web-workers to investigate independent parts of this task, then verify the important claims locally."
policy:
  allow_implicit_invocation: true
